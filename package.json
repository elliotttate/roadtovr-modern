{
  "name": "roadtovr-horizon",
  "version": "0.1.1",
  "private": true,
  "description": "A modern editorial redesign for Road to VR, delivered as a Chrome extension.",
  "scripts": {
    "check": "node --check src/bootstrap.js && node --check src/content.js && node --check popup/popup.js && node --check scripts/test-live.mjs && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:live": "node scripts/test-live.mjs",
    "package": "bash scripts/package.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
